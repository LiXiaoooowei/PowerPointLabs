<UserControl x:Class="PowerPointLabs.FYP.Views.CustomAnimationItemCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PowerPointLabs.FYP.Data"
             mc:Ignorable="d">
    <Grid>
       
        <ListView x:Name="listView" ItemsSource="{Binding Items}"
                  BorderThickness="0" Margin="0" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type local:CustomAnimationItem}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="290" />
                            <ColumnDefinition Width="150" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding ShapeName}"/>
                        <TextBlock Grid.Column="1" Text="{Binding EffectName}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
